// import { getParetoFront } from './findBestSchedules';

test('dummy', () => {
  expect(0).toBe(0);
});

// test('Pareto Optimum 1', () => {
//   expect(
//     getParetoFront([
//       [{ quality: { totalHourDifference: 1, shiftDistribution: 1 } }],
//       [{ quality: { totalHourDifference: 1, shiftDistribution: 1 } }],
//     ])
//   ).toStrictEqual([
//     [{ quality: { totalHourDifference: 1, shiftDistribution: 1 } }],
//   ]);
// });

// test('Pareto Optimum 2', () => {
//   const result = getParetoFront([
//     [{ quality: { totalHourDifference: 1, shiftDistribution: 2 } }],
//     [{ quality: { totalHourDifference: 1, shiftDistribution: 1 } }],
//   ]);

//   expect(result).toStrictEqual([
//     [{ quality: { totalHourDifference: 1, shiftDistribution: 1 } }],
//   ]);
// });
